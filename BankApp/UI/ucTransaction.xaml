<UserControl x:Class="BankApp.UI.ucTransaction"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankApp.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="LightGray">
        <TextBlock Text="{Binding BillFrom}"></TextBlock>
        <TextBlock Text="{Binding BillTo}"></TextBlock>
        <TextBlock Text="{Binding Transaction}"></TextBlock>
        <TextBlock Text="{Binding Amount}"></TextBlock>
        <TextBlock Text="{Binding DateTime}"></TextBlock>
    </StackPanel>
</UserControl>
