<Window x:Class="BankApp.UI.ClientUI.Winds.WndBillSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BankApp.UI.ClientUI.Winds"
        mc:Ignorable="d"
        Title="Банк - Настройка счетов"
        Height="450" Width="800"
        MinHeight="450" MinWidth="800">
    <Grid>
        <TextBlock x:Name="TblSaving" Visibility="Collapsed">Сохраняем настройки</TextBlock>
        <StackPanel x:Name="SpContent">
            <Grid x:Name="GrSettings">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
            </Grid>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnSave" Click="BtnSave_Click" Width="200">Сохранить</Button>
                <Button x:Name="BtnExit" Click="BtnExit_Click" Width="200">Закрыть</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
